import { Typography } from '@material-ui/core';

const View = () => {
  return (
    <div className="MyDeals">
      <Typography variant="h4">
        Create a new deal
      </Typography>
    </div>
  );
};

export default View;
