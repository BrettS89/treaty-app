import { Typography } from '@material-ui/core';

const View = () => {
  return (
    <div style={{ padding: 20 }}>
      <Typography variant="h4">
        Dashboard
      </Typography>
    </div>
  );
};

export default View;
