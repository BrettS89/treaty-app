import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
  pageTitle: {
    fontWeight: 600,
    marginBottom: 20,
  },
  title: {
    fontWeight: 600,
  },
  company: {
    fontStyle: 'italic'
  },
  broker: {
    fontSize: 13
  }
});

export default useStyles;
