import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
  followButton: {
  },
  summaryTitles: {
    fontWeight: 600,
    marginBottom: 10,
    color: 'grey',
  },
  detailsSectionTitle: {
    marginBottom: 10,
    fontWeight: 600
  },
  detailsSectionKey: {
    fontWeight: 600,
    color: 'grey'
  }
});

export default useStyles;
