import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
  followButton: {
    fontWeight: 500,
  },
  summaryTitles: {
    fontWeight: 600,
    marginBottom: 10,
  },
  detailsSectionTitle: {
    marginBottom: 10,
    fontWeight: 600
  },
  detailsSectionKey: {
    fontWeight: 600
  }
});

export default useStyles;
